//@version=6
strategy("BOS Strong Strategy", max_lines_count = 500, overlay = true, calc_on_every_tick = true)

// ================================================================================
// 🔑 CHIẾN LƯỢC BOS → OB → ENTRY
// ================================================================================
// 
// TRÌNH TỰ THỰC HIỆN:
// 1. BOS (Break of Structure) - Xác nhận xu hướng mới
// 2. OB (Order Block) - Tìm điểm entry tối ưu  
// 3. Entry - Vào lệnh khi giá retracement về OB
//
// CHI TIẾT TỪNG BƯỚC:
//
// 📍 BƯỚC 1: BOS (Break of Structure)
// - BOS xảy ra khi giá phá swing high/low quan trọng
// - Đây chỉ là xác nhận xu hướng mới hình thành, CHƯA ENTRY
// - BOS xuống = giá đóng cửa phá swing low gần nhất
// - BOS lên = giá đóng cửa phá swing high gần nhất
//
// 📍 BƯỚC 2: Tìm OB (Order Block) 
// - Sau khi có BOS, tìm OB gần nhất theo hướng BOS
// - Nếu BOS xuống → chọn Bearish OB (cây bullish cuối cùng trước cú giảm mạnh)
// - Nếu BOS lên → chọn Bullish OB (cây bearish cuối cùng trước cú tăng mạnh)
//
// 📍 BƯỚC 3: Entry Point
// - Đợi giá hồi (retracement) về OB đó
// - Đây chính là điểm "entry vàng"
// - Thường đặt limit order tại vùng OB (hoặc wick của OB)
// - ⚠️ QUAN TRỌNG: Không entry ngay khi vừa có BOS
//
// 📍 RISK MANAGEMENT:
// - SL đặt ngoài OB với buffer (trên cho short, dưới cho long)
// - TP theo R:R (1:1.5 mặc định cho entry tốt hơn)
// - Buffer SL để tránh bị hunt bởi market makers
//
// ================================================================================

// === Strategy Settings ===
// BOS Strong Entry Settings
showBOSSignals = input.bool(true, title="Show BOS Signals", group="BOS Entry")
showSLTP = input.bool(true, title="Show SL/TP Levels", group="Risk Management")
showOBZones = input.bool(true, title="Show Order Block Zones", group="BOS Entry")

// Entry Mode Settings (Dựa trên thống kê winrate cao)
entryMode = input.string("Retracement", title="Entry Mode", options=["Immediate", "Retracement", "Hybrid"], group="Entry Strategy", tooltip="Immediate: Entry ngay tại OB (winrate cao)\nRetracement: Chờ giá hồi về OB\nHybrid: 50% ngay + 50% chờ retracement")
waitForRetracement = input.bool(true, title="Wait for OB Retracement", group="BOS Entry")
retracementThreshold = input.float(0.5, title="Retracement Threshold", minval=0.1, maxval=1.0, step=0.1, group="BOS Entry")

// Risk Management Settings
slLookback = input.int(50, title="SL Lookback (bars)", minval=10, maxval=200, group="Risk Management")
tpRatio = input.float(1.5, title="TP Ratio (vs SL)", minval=0.1, maxval=3.0, step=0.1, group="Risk Management")
slBuffer = input.float(0.1, title="SL Buffer (%)", minval=0.0, maxval=1.0, step=0.05, group="Risk Management", tooltip="Buffer to avoid SL hunting (% of price)")

// === Constants ===
BULLISH = +1
BEARISH = -1
BOS = "BOS"
CHOCH = "CHOCH"

GREEN = #089981
RED = #F23645
BLUE = #2157f3
GRAY = #878b94

// === Data Structures ===
type pivot
    float currentLevel
    float lastLevel
    bool crossed
    int barTime
    int barIndex

type trend
    int bias

type orderBlock
    float high
    float low
    int barIndex
    int direction  // 1 for bullish OB, -1 for bearish OB
    bool active
    bool touched

type bosState
    bool bullishBOS
    bool bearishBOS
    float obHigh
    float obLow
    bool waitingForRetracement

// === Variables ===
var pivot swingHigh = pivot.new(na, na, false, time, bar_index)
var pivot swingLow = pivot.new(na, na, false, time, bar_index)
var trend swingTrend = trend.new(0)
var bosState currentBOS = bosState.new(false, false, na, na, false)
var orderBlock currentOB = orderBlock.new(na, na, na, 0, false, false)

// === Smart Money Functions ===
// Leg detection function
leg(int size) =>
    var leg = 0
    newLegHigh = high[size] > ta.highest(high, size)
    newLegLow = low[size] < ta.lowest(low, size)
    
    if newLegHigh
        leg := 0  // Bearish leg
    else if newLegLow
        leg := 1  // Bullish leg
    leg

startOfNewLeg(int leg) => ta.change(leg) != 0
startOfBearishLeg(int leg) => ta.change(leg) == -1
startOfBullishLeg(int leg) => ta.change(leg) == +1

// Get current structure
getCurrentStructure(int size) =>
    currentLeg = leg(size)
    newPivot = startOfNewLeg(currentLeg)
    pivotLow = startOfBullishLeg(currentLeg)
    pivotHigh = startOfBearishLeg(currentLeg)

    if newPivot
        if pivotLow
            swingLow.lastLevel := swingLow.currentLevel
            swingLow.currentLevel := low[size]
            swingLow.crossed := false
            swingLow.barTime := time[size]
            swingLow.barIndex := bar_index[size]
        else
            swingHigh.lastLevel := swingHigh.currentLevel
            swingHigh.currentLevel := high[size]
            swingHigh.crossed := false
            swingHigh.barTime := time[size]
            swingHigh.barIndex := bar_index[size]



// === BOS Detection ===
// ================================================================================
// 🎯 BOS DETECTION LOGIC
// ================================================================================
// Theo chiến lược BOS → OB → Entry:
// 1. Phát hiện khi giá phá swing high/low quan trọng
// 2. Xác nhận bằng cây nến mạnh (strong bar)  
// 3. Đánh dấu xu hướng mới nhưng CHƯA ENTRY
// 4. Chờ retracement về OB để entry
// ================================================================================

// Check for strong bullish/bearish bars
isStrongBullishBar() =>
    bodySize = math.abs(close - open)
    upperWick = high - math.max(close, open)
    lowerWick = math.min(close, open) - low
    
    // Strong bullish: body > 60% of total range, small upper wick
    totalRange = high - low
    bodyRatio = bodySize / totalRange
    upperWickRatio = upperWick / totalRange
    
    bodyRatio > 0.5 and upperWickRatio < 0.4

isStrongBearishBar() =>
    bodySize = math.abs(close - open)
    upperWick = high - math.max(close, open)
    lowerWick = math.min(close, open) - low
    
    // Strong bearish: body > 60% of total range, small lower wick
    totalRange = high - low
    bodyRatio = bodySize / totalRange
    lowerWickRatio = lowerWick / totalRange
    
    bodyRatio > 0.5 and lowerWickRatio < 0.4




// Check for weak bullish/bearish bars (any bullish/bearish bar)
isWeakBullishBar() =>
    close > open

isWeakBearishBar() =>
    close < open

// ================================================================================
// 🎯 ORDER BLOCK DETECTION FUNCTIONS
// ================================================================================
// Tìm Order Block theo chiến lược BOS → OB → Entry:
// - Bullish OB: Cây bearish cuối cùng trước cú tăng mạnh
// - Bearish OB: Cây bullish cuối cùng trước cú giảm mạnh
// ================================================================================

// Tìm Bullish Order Block (cho entry long sau BOS lên)
findBullishOB() =>
    var float obHigh = na
    var float obLow = na
    var int obIndex = na
    
    // Tìm cây bearish cuối cùng trước cú tăng mạnh
    for i = 1 to 50
        if close[i] < open[i] and close[i-1] > open[i-1]  // Cây bearish sau cây bullish
            // Kiểm tra có cú tăng mạnh sau đó không
            hasStrongRise = false
            for j = 0 to i-1
                if close[j] > high[i] and isStrongBullishBar()
                    hasStrongRise := true
                    break
            
            if hasStrongRise
                obHigh := high[i]
                obLow := low[i]
                obIndex := bar_index[i]
                break
    
    [obHigh, obLow, obIndex]

// Tìm Bearish Order Block (cho entry short sau BOS xuống)  
findBearishOB() =>
    var float obHigh = na
    var float obLow = na
    var int obIndex = na
    
    // Tìm cây bullish cuối cùng trước cú giảm mạnh
    for i = 1 to 50
        if close[i] > open[i] and close[i-1] < open[i-1]  // Cây bullish sau cây bearish
            // Kiểm tra có cú giảm mạnh sau đó không
            hasStrongDrop = false
            for j = 0 to i-1
                if close[j] < low[i] and isStrongBearishBar()
                    hasStrongDrop := true
                    break
                    
            if hasStrongDrop
                obHigh := high[i]
                obLow := low[i] 
                obIndex := bar_index[i]
                break
    
    [obHigh, obLow, obIndex]

// Kiểm tra giá có retracement về OB zone không
isPriceInOBZone(float obHigh, float obLow) =>
    low <= obHigh and high >= obLow

// ================================================================================
// 🚨 BOS SIGNAL DETECTION
// ================================================================================
// LOGIC THEO CHIẾN LƯỢC:
// - BOS lên: giá đóng cửa phá swing high gần nhất + cây nến mạnh
// - BOS xuống: giá đóng cửa phá swing low gần nhất + cây nến mạnh  
// - Đây chỉ là bước 1 - XÁC NHẬN xu hướng mới
// - Sau BOS → tìm OB gần nhất → chờ retracement để entry
// ================================================================================
detectBOSSignals() =>
    bullishBOSStrong = false
    bearishBOSStrong = false
    
    // 📈 BOS LÊN: Giá phá swing high + cây nến bullish mạnh
    // → Xu hướng tăng mới, tìm Bullish OB và chờ retracement
    if ta.crossover(close, swingHigh.currentLevel) and not swingHigh.crossed and isStrongBullishBar()
        signalType = swingTrend.bias == BEARISH ? CHOCH : BOS
        
        if signalType == BOS
            bullishBOSStrong := true
            swingHigh.crossed := true
            swingTrend.bias := BULLISH
            
            // Tìm Bullish OB và thực hiện entry theo mode
            [obHigh, obLow, obIndex] = findBullishOB()
            if not na(obHigh) and not na(obLow)
                currentBOS.bullishBOS := true
                currentBOS.bearishBOS := false
                currentBOS.obHigh := obHigh
                currentBOS.obLow := obLow
                
                // Thực hiện entry theo mode được chọn
                if entryMode == "Immediate"
                    executeImmediateEntry(true, obHigh, obLow)
                else if entryMode == "Hybrid"
                    executeHybridEntry(true, obHigh, obLow)
                else  // Retracement mode
                    currentBOS.waitingForRetracement := true
                    currentOB.high := obHigh
                    currentOB.low := obLow
                    currentOB.barIndex := obIndex
                    currentOB.direction := 1  // Bullish OB
                    currentOB.active := true
                    currentOB.touched := false
    
    // 📉 BOS XUỐNG: Giá phá swing low + cây nến bearish mạnh  
    // → Xu hướng giảm mới, tìm Bearish OB và chờ retracement
    if ta.crossunder(close, swingLow.currentLevel) and not swingLow.crossed and isStrongBearishBar()
        signalType = swingTrend.bias == BULLISH ? CHOCH : BOS
        
        if signalType == BOS
            bearishBOSStrong := true
            swingLow.crossed := true
            swingTrend.bias := BEARISH
            
            // Tìm Bearish OB và thực hiện entry theo mode
            [obHigh, obLow, obIndex] = findBearishOB()
            if not na(obHigh) and not na(obLow)
                currentBOS.bullishBOS := false
                currentBOS.bearishBOS := true
                currentBOS.obHigh := obHigh
                currentBOS.obLow := obLow
                
                // Thực hiện entry theo mode được chọn
                if entryMode == "Immediate"
                    executeImmediateEntry(false, obHigh, obLow)
                else if entryMode == "Hybrid"
                    executeHybridEntry(false, obHigh, obLow)
                else  // Retracement mode
                    currentBOS.waitingForRetracement := true
                    currentOB.high := obHigh
                    currentOB.low := obLow
                    currentOB.barIndex := obIndex
                    currentOB.direction := -1  // Bearish OB
                    currentOB.active := true
                    currentOB.touched := false
    
    [bullishBOSStrong, bearishBOSStrong]

// === Risk Management Functions ===
// Calculate TP based on 100% of SL distance
calculateTP(float entryPrice, float slPrice, bool isLong) =>
    slDistance = math.abs(entryPrice - slPrice)
    tpDistance = slDistance * tpRatio
    
    if isLong
        entryPrice + tpDistance
    else
        entryPrice - tpDistance

// === Entry Functions ===
// Immediate Entry tại OB đầu tiên sau BOS (winrate cao)
executeImmediateEntry(bool isLong, float obHigh, float obLow) =>
    if isLong
        entryPrice = obHigh  // Entry tại OB High cho long
        slPrice = obLow * (1 - slBuffer/100)  // SL dưới OB với buffer
        tpPrice = calculateTP(entryPrice, slPrice, true)
        
        strategy.entry("Long Immediate OB", strategy.long)
        strategy.exit("Long Immediate Exit", "Long Immediate OB", stop=slPrice, limit=tpPrice)
        
        if showBOSSignals
            label.new(bar_index, low, text="ENTRY\nIMMEDIATE\nLONG", color=color.green, style=label.style_label_up, size=size.small)
    else
        entryPrice = obLow  // Entry tại OB Low cho short
        slPrice = obHigh * (1 + slBuffer/100)  // SL trên OB với buffer
        tpPrice = calculateTP(entryPrice, slPrice, false)
        
        strategy.entry("Short Immediate OB", strategy.short)
        strategy.exit("Short Immediate Exit", "Short Immediate OB", stop=slPrice, limit=tpPrice)
        
        if showBOSSignals
            label.new(bar_index, high, text="ENTRY\nIMMEDIATE\nSHORT", color=color.red, style=label.style_label_down, size=size.small)

// Hybrid Entry: 50% ngay + 50% chờ retracement
executeHybridEntry(bool isLong, float obHigh, float obLow) =>
    if isLong
        // 50% entry ngay
        entryPrice1 = obHigh
        slPrice1 = obLow * (1 - slBuffer/100)
        tpPrice1 = calculateTP(entryPrice1, slPrice1, true)
        
        strategy.entry("Long Hybrid 1", strategy.long, qty=0.5)
        strategy.exit("Long Hybrid Exit 1", "Long Hybrid 1", stop=slPrice1, limit=tpPrice1)
        
        // 50% chờ retracement
        currentBOS.waitingForRetracement := true
        currentOB.active := true
        currentOB.high := obHigh
        currentOB.low := obLow
        currentOB.direction := 1
        
        if showBOSSignals
            label.new(bar_index, low, text="HYBRID\n50% NOW", color=color.blue, style=label.style_label_up, size=size.small)
    else
        // 50% entry ngay
        entryPrice1 = obLow
        slPrice1 = obHigh * (1 + slBuffer/100)
        tpPrice1 = calculateTP(entryPrice1, slPrice1, false)
        
        strategy.entry("Short Hybrid 1", strategy.short, qty=0.5)
        strategy.exit("Short Hybrid Exit 1", "Short Hybrid 1", stop=slPrice1, limit=tpPrice1)
        
        // 50% chờ retracement
        currentBOS.waitingForRetracement := true
        currentOB.active := true
        currentOB.high := obHigh
        currentOB.low := obLow
        currentOB.direction := -1
        
        if showBOSSignals
            label.new(bar_index, high, text="HYBRID\n50% NOW", color=color.orange, style=label.style_label_down, size=size.small)

// === Main Execution ===
// Update structure
getCurrentStructure(50)  // Swing structure

// Detect BOS signals
[bullishBOSStrong, bearishBOSStrong] = detectBOSSignals()

// ================================================================================
// 🎯 ORDER BLOCK (OB) LOGIC - BƯỚC 2 CỦA CHIẾN LƯỢC
// ================================================================================
// CÁCH HOẠT ĐỘNG:
// 1. Sau khi có BOS, tìm OB gần nhất theo hướng BOS
// 2. BOS xuống → Bearish OB (cây bullish cuối cùng trước cú giảm mạnh)
// 3. BOS lên → Bullish OB (cây bearish cuối cùng trước cú tăng mạnh)  
// 4. Đợi giá retracement về vùng OB này để entry
// 5. SL đặt ngoài OB (trên cho short, dưới cho long)
// ================================================================================

m5High = request.security(syminfo.tickerid, "5", high)
m5Low = request.security(syminfo.tickerid, "5", low)
m5Volume = request.security(syminfo.tickerid, "5", volume)

// Xác định Order Block từ timeframe 5M
m5OBHigh = request.security(syminfo.tickerid, "5", ta.highest(high, 50))
m5OBLow = request.security(syminfo.tickerid, "5", ta.lowest(low, 50))

// 🎯 SL LEVELS - Theo nguyên tắc OB
longSLLevel = m5OBLow    // SL cho long = dưới OB (thấp nhất của OB 5M)
shortSLLevel = m5OBHigh  // SL cho short = trên OB (cao nhất của OB 5M)

// ================================================================================
// 🚀 STRATEGY EXECUTION - BƯỚC 3: ENTRY POINT (CẢI THIỆN)
// ================================================================================
// ✅ LOGIC ENTRY THEO CHIẾN LƯỢC BOS → OB → ENTRY CHUẨN:
// 1. ✅ Phát hiện BOS → Đánh dấu xu hướng mới
// 2. ✅ Tìm OB gần nhất theo hướng BOS  
// 3. ✅ Chờ giá retracement về vùng OB
// 4. ✅ Entry khi giá touch OB zone (điểm entry vàng)
// 
// 🎯 RISK MANAGEMENT:
// - SL: Ngoài OB với buffer (dưới cho long, trên cho short)
// - TP: Theo R:R ratio (mặc định 1:1.5 cho entry tốt hơn)
// ================================================================================

// Kiểm tra retracement về OB zone sau BOS
if currentBOS.waitingForRetracement and currentOB.active
    inOBZone = isPriceInOBZone(currentOB.high, currentOB.low)
    
    // ✅ Entry khi giá retracement về OB zone
    if inOBZone and not currentOB.touched
        currentOB.touched := true
        
        // 📈 LONG ENTRY: Sau BOS lên và retracement về Bullish OB
        if currentBOS.bullishBOS and strategy.position_size <= 0
            entryPrice = close
            slPrice = currentOB.low * (1 - slBuffer/100)  // SL dưới OB với buffer
            tpPrice = calculateTP(entryPrice, slPrice, true)
            
            strategy.entry("Long OB Retracement", strategy.long)
            strategy.exit("Long OB Exit", "Long OB Retracement", stop=slPrice, limit=tpPrice)
            
            if showBOSSignals
                label.new(bar_index, low, text="ENTRY\nOB\nLONG", color=color.green, style=label.style_label_up, size=size.small)
            
            // Reset BOS state after entry
            currentBOS.waitingForRetracement := false
            currentOB.active := false
        
        // 📉 SHORT ENTRY: Sau BOS xuống và retracement về Bearish OB
        if currentBOS.bearishBOS and strategy.position_size >= 0
            entryPrice = close
            slPrice = currentOB.high * (1 + slBuffer/100)  // SL trên OB với buffer
            tpPrice = calculateTP(entryPrice, slPrice, false)
            
            strategy.entry("Short OB Retracement", strategy.short)
            strategy.exit("Short OB Exit", "Short OB Retracement", stop=slPrice, limit=tpPrice)
            
            if showBOSSignals
                label.new(bar_index, high, text="ENTRY\nOB\nSHORT", color=color.red, style=label.style_label_down, size=size.small)
            
            // Reset BOS state after entry
            currentBOS.waitingForRetracement := false
            currentOB.active := false

// 🔄 Hiển thị BOS signals theo mode
if bullishBOSStrong and showBOSSignals
    if entryMode == "Immediate"
        label.new(bar_index, low, text="BOS↗\nIMMEDIATE", color=color.green, style=label.style_label_up, size=size.normal)
    else if entryMode == "Hybrid"
        label.new(bar_index, low, text="BOS↗\nHYBRID", color=color.blue, style=label.style_label_up, size=size.normal)
    else
        label.new(bar_index, low, text="BOS↗\nWAIT OB", color=color.blue, style=label.style_label_up, size=size.normal)

if bearishBOSStrong and showBOSSignals  
    if entryMode == "Immediate"
        label.new(bar_index, high, text="BOS↘\nIMMEDIATE", color=color.red, style=label.style_label_down, size=size.normal)
    else if entryMode == "Hybrid"
        label.new(bar_index, high, text="BOS↘\nHYBRID", color=color.orange, style=label.style_label_down, size=size.normal)
    else
        label.new(bar_index, high, text="BOS↘\nWAIT OB", color=color.orange, style=label.style_label_down, size=size.normal)

// === Plots ===
// Calculate TP levels for plotting
longTPLevel = strategy.position_size > 0 ? calculateTP(strategy.position_avg_price, longSLLevel, true) : na
shortTPLevel = strategy.position_size < 0 ? calculateTP(strategy.position_avg_price, shortSLLevel, false) : na

// Plot current SL levels for reference
plot(showSLTP and strategy.position_size > 0 ? longSLLevel : na, "Long SL Level", color.red, 2, plot.style_stepline)
plot(showSLTP and strategy.position_size < 0 ? shortSLLevel : na, "Short SL Level", color.red, 2, plot.style_stepline)

// Plot current TP levels for reference
plot(longTPLevel, "Long TP Level", color.green, 2, plot.style_stepline)
plot(shortTPLevel, "Short TP Level", color.green, 2, plot.style_stepline)

// ================================================================================
// 📊 ORDER BLOCK ZONE VISUALIZATION
// ================================================================================
// Hiển thị vùng OB đang chờ retracement
// ================================================================================

// Plot Order Block zones
obHighLevel = currentOB.active ? currentOB.high : na
obLowLevel = currentOB.active ? currentOB.low : na

plot(showOBZones and currentOB.active ? obHighLevel : na, "OB High", color.new(color.purple, 50), 2, plot.style_stepline)
plot(showOBZones and currentOB.active ? obLowLevel : na, "OB Low", color.new(color.purple, 50), 2, plot.style_stepline)

// Fill OB zone
fill(plot(showOBZones and currentOB.active ? obHighLevel : na, color=na), 
     plot(showOBZones and currentOB.active ? obLowLevel : na, color=na), 
     color=color.new(currentOB.direction > 0 ? color.green : color.red, 90),
     title="OB Zone")

// ================================================================================
// 🎉 CHIẾN LƯỢC BOS → OB → ENTRY ĐÃ HOÀN THIỆN
// ================================================================================
// ✅ SCRIPT ĐÃ CẢI THIỆN HOÀN TOÀN:
// 1. ✅ BOS Detection - Phát hiện khi giá phá swing high/low với cây nến mạnh
// 2. ✅ Order Block Detection - Tìm cây nến cụ thể làm OB theo đúng methodology
//    • Bullish OB: Cây bearish cuối trước cú tăng mạnh (cho long entry)  
//    • Bearish OB: Cây bullish cuối trước cú giảm mạnh (cho short entry)
// 3. ✅ Retracement Logic - Chờ giá hồi về OB zone rồi mới entry
// 4. ✅ Risk Management - SL ngoài OB, TP theo R:R 1:1
// 5. ✅ Visual Signals - Hiển thị BOS, OB zones, entry points
// 6. ✅ Settings Panel - Tùy chỉnh các tham số strategy
//
// 🎯 CÁCH HOẠT ĐỘNG THEO CHIẾN LƯỢC CHUẨN:
// 1. 🔍 Phát hiện BOS → Hiển thị "BOS↗ WAIT OB" hoặc "BOS↘ WAIT OB"
// 2. 🎯 Tìm OB gần nhất theo hướng BOS → Hiển thị vùng OB màu tím
// 3. ⏳ Chờ giá retracement về vùng OB (điểm entry vàng)
// 4. 🚀 Entry khi giá touch OB → Hiển thị "ENTRY OB LONG/SHORT"
// 5. 📊 SL đặt ngoài OB với buffer, TP theo R:R ratio 1:1.5
//
// 🎮 SETTINGS AVAILABLE:
// • Show BOS Signals, Show OB Zones, Show SL/TP
// • Entry Mode: Immediate (winrate cao), Retracement, Hybrid
// • Retracement Threshold, TP Ratio, SL Buffer
//
// 💎 ĐIỂM MẠNH CỦA STRATEGY:
// • 3 Entry Modes linh hoạt: Immediate (winrate cao), Retracement, Hybrid
// • Immediate Entry: Bắt momentum mạnh nhất ngay tại OB đầu tiên
// • Retracement Entry: Chờ giá hồi về OB để có entry price tốt hơn
// • Hybrid Entry: 50% ngay + 50% chờ retracement (tối ưu cả 2)
// • Risk management chặt chẽ với SL ngoài OB + buffer
// • Visual clear giúp theo dõi signals dễ dàng
// ================================================================================